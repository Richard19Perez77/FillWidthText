# FillWidthText

Small Jetpack Compose sample for text that fills the available width. Logic lives in `MainActivity.kt`.

## Stack

Kotlin, Jetpack Compose.

## Run

Open the project in Android Studio and run the `app` configuration.

```bash
./gradlew :app:assembleDebug
```
